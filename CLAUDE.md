# CLAUDE.md

このファイルは Claude Code がセッション開始時に自動で読み込む。
新しいセッションで作業を始める前に、必ずこのファイル全体と
`docs/implementation-plan.md` の「進捗状況」セクションを読み、
現在どこまで着手済みかを把握してから作業を始めること。

## プロジェクト概要

- **tatekae-app（立て替え管理）**: 立て替えたお金を記録・管理する個人向け PWA
- 詳細な要件・データモデル・実装フェーズは [docs/implementation-plan.md](docs/implementation-plan.md) が正
- 技術スタック: Vite + React 19 + TypeScript、PWA化には `vite-plugin-pwa`
- パッケージマネージャーは **pnpm** に統一済み（`npm` / `yarn` は使わない）
- Lint は **oxlint**（`pnpm run lint`）。当初計画のPrettier+ESLintから変更済み

## 作業引き継ぎルール（重要・毎回必須）

他セッションへの引き継ぎを前提に作業する。以下を**都度**（1つの意味のあるまとまり作業ごと。
Phase単位に限らず、方針変更・仕様変更・調査結果なども含む）実施すること。

1. **作業の区切りごとに `docs/implementation-plan.md` を更新する**
   - 冒頭の「進捗状況」セクションのPhase一覧を更新（✅完了 / 🔧作業中 / ⬜未着手）
   - 完了した内容・検証結果（ビルド確認、テスト実行結果など）を「Phase N 完了内容」として追記
   - 当初計画から変更した点（技術選定の変更、ユーザー指示による順序変更など）を「当初計画からの変更点」に追記
   - 「次にやること」を必ず最新の状態に書き換える
   - 第8章のチェックリストも実態に合わせて `[x]` / `[ ]` を更新する
   - 未検証・未実施の項目は正直に「未検証」「未実行」と明記し、過大申告しない
2. **ドキュメント更新はコード変更と同じコミット、または直後の別コミットに含める**
   - 作業内容だけ実装してドキュメント更新を後回しにしない
3. **セッションの最後（ユーザーとのやり取りが一区切りついた時点）に、進捗状況セクションが最新か再確認する**
4. `docs/implementation-plan.md` 以外に一時的なメモや調査ログを作らない。恒久的に残す情報は全てこのファイルに集約する

## その他の規約

- git のコミット・プッシュ先ブランチは指示されたブランチに従う
- 破壊的操作（force push、履歴書き換え等）は明示的な許可がない限り行わない
